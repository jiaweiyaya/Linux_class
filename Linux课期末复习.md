帮我编写一个shell脚本，此程序的功能是：

---

#### 1.使用循环语句实现在终端上输出如下图所示的99乘法表。要求程序允许得出正确结果，写出会话过程截图（截图即可）。
1
2 4
3 6 9
4 8 12 16
5 10 15 20 25
6 12 18 24 30 36
7 14 21 28 35 42 49
8 16 24 32 40 48 56 64
9 18 27 36 45 54 63 72 81

---

#### 2.一个循环菜单程序，每个菜单项由一个子函数来完成其功能，菜单内容是：
##### 1) add: 计算从键盘输入的两个整数之和
##### 2) sub: 计算从键盘输入的两个整数之差
##### 3) mul: 计算从键盘输入的两个整数之积
##### 4) sub: 计算从键盘输入的两个整数之商
#### 要求该菜单程序能够循环运行，直到按字母Q才能退出程序。要求程序运行得出正确结果，写出会话过程（截图即可）。

---

#### 3.用C语言编写两个函数，其中一个要求半径为r的圆形的周长，另一个求半径为r的圆形的面积。将这两个函数的实现放在`circlefuns.c`文件中，并为其建立相应的头文件`circlefuns.h`。
##### 1) 建立一个静态库`libmycircle.a`以包含上述两个函数。创建一个测试文件`test.c`，该文件调用上述静态库中的函数以分别计算从终端输入的半径对应圆的周长与面积，并把结果显示出来。直接在命令行编译，并运行。
##### 2) 改用用make工具编译第1)步的程序。写出相应的`Makefile`，执行编译，并执行程序。
##### 3) 把静态库换成动态库`libmycircle.so`，完成上述步骤。要求程序运行得出正确结果，写出会话过程（截图即可）